# 談話室預約管理系統 (Meeting Room Booking System)

這是一個基於 React 和 Firebase 的會議室預約管理系統，適用於觀護所等機構的談話室排程管理。

## 功能特色

- 📅 月曆視圖顯示預約狀態
- 👥 人員管理（觀護人與心理師）
- ⏰ 固定排班設定
- 📝 臨時預約功能
- 📊 報表彙整
- ☁️ Firebase 雲端同步
- 💾 資料匯入/匯出功能

## 前置需求

- Node.js 18.0 或以上版本
- Firebase 專案（需要設定 Firestore 資料庫和 Authentication）

## 安裝步驟

1. 安裝依賴套件：
```bash
npm install
```

2. 設定 Firebase 配置：
   - 複製 `.env.example` 為 `.env`
   - 在 Firebase Console 取得專案配置資訊
   - 將配置資訊填入 `.env` 檔案（注意：環境變數需使用 `VITE_` 前綴）

```bash
cp .env.example .env
# 編輯 .env 並填入您的 Firebase 配置
# 範例：
# VITE_FIREBASE_CONFIG={"apiKey":"...","authDomain":"...","projectId":"...","storageBucket":"...","messagingSenderId":"...","appId":"..."}
# VITE_APP_ID=my-app-id
```

## 執行應用程式

### 開發模式
```bash
npm run dev
```
應用程式將在 http://localhost:5173 啟動

### 建置生產版本
```bash
npm run build
```
建置檔案將輸出至 `dist` 目錄

### 預覽生產版本
```bash
npm run preview
```

## Firebase 設定

### 1. 建立 Firebase 專案
1. 前往 [Firebase Console](https://console.firebase.google.com/)
2. 建立新專案或選擇現有專案
3. 啟用 Firestore Database
4. 啟用 Authentication（Anonymous 登入）

### 2. 取得配置資訊
1. 在 Firebase Console 中，前往專案設定
2. 在「您的應用程式」區段選擇網頁應用程式
3. 複製 firebaseConfig 物件
4. 將配置資訊填入 `.env` 檔案

### 3. Firestore 安全規則
建議在 Firestore 中設定以下安全規則：

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /artifacts/{appId}/public/{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
```

## 使用說明

### 人員管理
- 系統預設建立 19 位觀護人（PO01-PO19）和 20 位心理師（PS01-PS20）
- 可在「人員管理」頁面設定每位人員的姓名

### 固定排班
- 在「固定排班」頁面設定每週固定的談話室使用排程
- 支援上午/下午時段

### 預約管理
- 在「預約現況」頁面查看月曆並進行臨時預約
- 固定排班會自動顯示在對應日期
- 假日會以灰色標示

### 刪除密碼
- 刪除排班或預約需要輸入密碼
- 預設密碼：`202655688`
- 可在 `src/App.jsx` 中修改 `DELETE_PASSWORD` 常數

## 技術架構

- **前端框架**: React 18
- **UI 庫**: Tailwind CSS
- **圖示**: Lucide React
- **後端服務**: Firebase (Firestore + Authentication)
- **建置工具**: Vite

## 專案結構

```
booking-meeting-rooms/
├── src/
│   ├── App.jsx          # 主要應用程式元件
│   └── main.jsx         # 應用程式入口點
├── index.html           # HTML 模板
├── vite.config.js       # Vite 配置
├── package.json         # 專案依賴
├── .env.example         # 環境變數範例
└── README.md           # 專案說明文件
```

## 授權

此專案僅供學習和內部使用。
